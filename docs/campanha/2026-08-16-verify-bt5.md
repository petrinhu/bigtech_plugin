# BT-5 — verificação adversarial (porte canônico: piso early)

**Data da verificação:** 2026-08-17  
**Hora local (medida):** `17/08/26 - 00:25:04` (`date '+%d/%m/%y - %H:%M:%S'`)  
**Papel:** verificador de campanha — host Grok, papel [grok][mais recente]  
**Item:** `BT-5` (eliminar `solo`/headcount como porte; skill `/bigtech` + hooks + Cósimo alinhados; piso **early**)  
**DoD desta ordem de serviço:**

1. Porte canônico = `early|scale|bigtech` (não `solo`) no `argument-hint` da skill `/bigtech` e na tabela de portes
2. Alias `solo` → `early` documentado **e** hooks não rebaixam a `solo`
3. `rg` em `skills/` `hooks/` `agents/`: `solo` como **porte primário** peer de `early`/`scale` sem deprec = **0** (ou só em contexto deprec/alias/test)
4. pytest/preci relevantes verdes (citar HEAD e preci local)

**HEAD do plugin no início desta auditoria:** `4e8843f5b32ac2871bafe04cd1c8056487ed04c5` (`main` == `origin/main` naquele instante; working tree limpa)  
**HEAD no fecho (pai deste commit):** `5745960f4e57500a847330df54422d750acd9a86` (`docs(bt-6): verificação dual-authority` — concurrente; **não** toca blobs de porte)  
**Implementação auditada:** `7455adf4b6c7e77c7fdb3e3f8c0850940da0f403` (`fix(bt-5): eliminate solo/headcount as architectural porte (floor early)`; tag local já existente `campanha/w3-bt5-porte-early`)  
**Prova de produto:** `git archive 4e8843f` → `/var/tmp/bt5-verify-HEAD-4e8843f` (não a working tree). Blobs BT-5 em `5745960` **idênticos** (`SKILL.md` `06fbc17`, hooks `3720eab`/`2df3d56`, Cósimo `5fa86bb`).  
**Método:** re-medição em disco + objeto git + archive. Prosa de CHANGELOG / commit de implementação **não** foi aceita sem prova.

**Veredito global:** **PASS nos 4 itens.**  
**Decisão autônoma (confirmar retroativamente):** promover `BT-5` para **✅ Concluído** e Estado Auditado **✓**. Não é promoção de `BT-6` / `BT-7` / `BT-8` (permanecem 🔍). Sem push. Sem patch de produto (verificador ≠ implementer).

---

## Escopo e o que isto não é

- Fecha a **prova** de que o porte arquitetural publicado no plugin é `early | scale | bigtech`, com piso early, e que `solo` só existe como alias deprecado / fixture de teste.
- Fecha **preci local** e a suíte pytest no blob de HEAD.
- **Não** fecha TST-BT-1 (revalidação da campanha inteira) nem AUD-BT-1.
- **Não** é cutover de `~/.claude`. O vault/Cósimo vivo ainda pode ensinar rótulos STALE (inventário BT-6); isso é dual-authority, fora desta fatia.
- **Não** houve push, merge, tag de release nem alteração de outros IDs.

---

## Checklist DoD (item a item)

| # | Item | Veredito | Evidência âncora |
|---|---|:---:|---|
| 1 | Porte canônico = `early\|scale\|bigtech` no `argument-hint` e na tabela | **PASS** | `skills/bigtech/SKILL.md` L4 `argument-hint: … [--porte early\|scale\|bigtech]`; tabela Cósimo 3 linhas (`early`/`scale`/`bigtech`); mapa skill L67–73; `docs/ORG.md` §5; pipeline L408–413 |
| 2 | Alias `solo`→`early` documentado; hooks não rebaixam a `solo` | **PASS** | skill L18/L58/L104; Cósimo L42; `DEPRECATED_PORTE_ALIASES = {"solo": "early"}`; `VALID_PORTES = {early, scale, bigtech}`; `open(..., "r")` só leitura; testes de normalização verdes |
| 3 | `rg` porte primário peer sem deprec = 0 | **PASS** | 23 hits `\bsolo\b` em skills/hooks/agents; **0** primários; 1 peer-string só em **assert de ausência** no teste; `check_solo_primary` = 0 findings |
| 4 | pytest / preci relevantes verdes | **PASS** | archive HEAD: pytest **161 passed** (hooks 150 + scripts 11); `scripts/preci.sh` **PRE-CI PASS 10/10**; evals **10/10** incl. `FORBID-SOLO-ALIAS` |

Nenhum FAIL material. Residuais abaixo **não** impedem ✅.

---

## 1. Porte canônico no `argument-hint` e nas tabelas

### Skill `/bigtech` (blob HEAD `06fbc171686827e50ab9af2b3c591ca5780e0c17`)

SHA-256 (archive): `c1495d72c7044de670bacb30082e52e160344dcfddfebdd70ccd59898442cb1b` (9065 B).

Frontmatter medido:

```
argument-hint: "[caminho ou descrição do projeto] [--porte early|scale|bigtech] [--dispatch]"
```

`solo` **não** entra no enum do hint. A regra de ouro (L14) e o contrato do mapa (L45) usam `PORTE: <early | scale | bigtech>`. A tabela de mapeamento (L67–73) tem **quatro linhas de variante** e **três valores de porte**:

| Porte (Cósimo / bigtech) | Variante | Nível SDLC |
|---|---|---|
| early (minimalista / projeto pequeno) | Pipeline-Early | S0 / S1 |
| early (early-stage / PMF) | Pipeline-Lean | S2 |
| scale | Pipeline-Padrão | S3 |
| bigtech | Pipeline-Completo | S4 |

Não há linha `solo` / `Pipeline-Sprint`.

### Tabela de Cósimo (`agents/cosimo-chief-of-staff.md`, blob `5fa86bb67c8357145faa155a4aa317739c518081`)

SHA-256: `e389e5156e5ece59d91e6edede6aefcb1669bdbce8297fec23364d1d4174678a` (8762 B).

Linhas da tabela § «Classificação de porte»:

| Porte | Variante |
|---|---|
| **early** | Pipeline-Lean (ou Pipeline-Early para early minimalista) |
| **scale** | Pipeline-Padrão |
| **bigtech** | Pipeline-Completo |

Frontmatter `description:`: `early | scale | bigtech; piso early; headcount/capacity não define porte`. Template de mapa: `PORTE: <early | scale | bigtech>`.

`rg '^\| \*\*(early|scale|bigtech|solo)'` no agent: 3 linhas (`early`, `scale`, `bigtech`); **0** `solo`.

### Docs de governança (alinhamento, não DoD extra)

| Ficheiro | Prova |
|---|---|
| `docs/ORG.md` §5 | 4 variantes; porte `early \| scale \| bigtech`; «Headcount/capacity humana **não** define porte» |
| `docs/pipeline_release_1.0.md` L408–413 | mesma tabela; sem `Pipeline-Sprint`; sem linha solo |
| `docs/lideranca_pipeline_release.md` L94–97 | «não existe porte "solo"»; §5.1 é **Pipeline-Early** |
| `skills/proj_software/SKILL.md` L36, L341 | capacity é **nota**; mapeia S0–S4 a `early\|scale\|bigtech` |
| `skills/tab_pendencias/SKILL.md` L108 | «piso de porte = early»; anti-OE por complexidade, não headcount |

`git log --oneline 7455adf..HEAD --` nos ficheiros da implementação BT-5: **vazio**. O blob de HEAD **é** o blob da implementação.

**Item 1 = PASS.**

---

## 2. Alias `solo` → `early` e hooks que não rebaixam

### Documentação do alias

| Onde | Texto medido |
|---|---|
| skill L18 | `**Alias deprecado:** --porte solo é aceito e normalizado para early … Não grave porte=solo` |
| skill L58 | `normalize alias deprecado (solo → early + aviso)` |
| skill L104 | `porte=<early\|scale\|bigtech>`; `**Nunca** grave porte=solo`; hooks normalizam a **leitura** |
| Cósimo L42 | `Alias deprecado solo → normalizar para early (e avisar)` |
| `docs/ORG.md` L127 | `Alias deprecado solo → early` |

### Hooks (só leitura; normalizam **para** early)

| | `bigtech_porte_reminder.py` | `bigtech_reinforce.py` |
|---|---|---|
| blob HEAD | `3720eab4808c1372ebe69fddffe16f01886c6c16` | `2df3d566f7d9644ae5f1dfc7482bb32b78d3d121` |
| SHA-256 | `7a382a00db192c677b82c1d3412ecb741dfe455a7e4e28d3978b5efb1586de56` | `81344909e224712d7954f4239a6b69c237f026b70daef25a4fe7eaea207b15fd` |
| enum canônico | mensagens `early\|scale\|bigtech` | `VALID_PORTES = frozenset({"early", "scale", "bigtech"})` |
| alias | `DEPRECATED_PORTE_ALIASES = {"solo": "early"}` | idem |
| I/O do marcador | `open(..., "r")` apenas | idem |

`rg 'open\(|write_text|write\('` nos dois hooks: **só** `open(..., "r")`. Os hooks **não escrevem** `.bigtech-porte`; portanto **não podem** gravar `porte=solo` nem rebaixar um `scale`/`bigtech` para `solo`.

`normalize_porte("solo")` / `normalize_porte("SOLO")` → `"early"`. Marcador legado `porte=solo` é exposto como `porte=early` no `additionalContext`. Mensagens de lembrete/reforço/roteamento usam `early|scale|bigtech; piso early; headcount nao define porte`.

### Testes que **executam** o contrato (não só prosa)

| Teste | Assert medido |
|---|---|
| `test_main_classificado_alias_solo_normaliza_para_early` | `porte=early` no contexto; `porte=solo` **ausente**; `normalize_porte("solo")=="early"` |
| `test_main_lembra_bigtech_em_projeto_de_codigo_sem_porte` | `early\|scale\|bigtech` (ou `early`) presente; `"solo/early" not in ctx` |
| `test_read_porte_alias_solo_normaliza_para_early` | leitura `porte=solo` → `early`; caixa `SOLO` → `early`; `scale` permanece `scale` |
| `test_main_reforco_normaliza_marcador_solo_legado` | contexto `porte=early`; `porte=solo` **ausente** |

**Item 2 = PASS.** Hooks sobem (solo→early) ou preservam (`scale`/`bigtech`); nunca descem para `solo`.

---

## 3. `rg`: `solo` como porte primário = 0

Âmbito pedido: `skills/` + `hooks/` + `agents/` no archive de HEAD.

### Inventário de `\bsolo\b` (23 hits / 6 ficheiros)

| Path | n | Classificação |
|---|---:|---|
| `skills/bigtech/SKILL.md` | 3 | deprec/alias («Alias deprecado», «normalize alias», «Nunca grave porte=solo») |
| `agents/cosimo-chief-of-staff.md` | 1 | deprec/alias (L42) |
| `hooks/bigtech_porte_reminder.py` | 5 | deprec/alias + «lido como early» |
| `hooks/bigtech_reinforce.py` | 4 | deprec/alias |
| `hooks/tests/test_bigtech_porte_reminder.py` | 5 | **test** (fixture `porte=solo` + assert de normalização; L109 **nega** o peer antigo `solo/early`) |
| `hooks/tests/test_bigtech_reinforce.py` | 5 | **test** (idem) |

### Peer `early|solo|scale` sem deprec na mesma linha

Padrão (o mesmo de `scripts/check_semantic_drift.py` `RE_SOLO_PEER`):

```
(?:early|scale|bigtech)\s*[|/]\s*solo
|\bsolo\s*[|/]\s*(?:early|scale|bigtech)
|early\s*[|,]\s*solo\s*[|,]\s*scale
|\bsolo\s*[|,]\s*early\s*[|,]\s*scale
```

Único hit no âmbito: `hooks/tests/test_bigtech_porte_reminder.py:109` — `assert "solo/early" not in ctx`. É **teste** que exige a **ausência** da string-peer antiga. Não ensina `solo` como valor.

### Gate oficial `check_solo_primary` (paths de produto)

```
findings_porte_solo: 0
ok: True
```

O gate também varre `docs/**` de produto (exclui `docs/campanha/` e `docs/house/`). Zero findings.

**Item 3 = PASS.** `solo` como porte primário peer de `early`/`scale` sem deprec = **0**.

---

## 4. pytest / preci no HEAD

Comando: `git archive HEAD` → `/var/tmp/bt5-verify-HEAD-4e8843f`; Python 3.14.6.

| Suite | Resultado | rc |
|---|---|---:|
| `python3 -m pytest hooks/tests -q` | **150 passed** in 2.51s | 0 |
| `python3 -m pytest scripts/tests -q` | **11 passed** in 0.29s | 0 |
| `python3 evals/bigtech_routing/run_evals.py` | **10/10 PASS** (`FORBID-SOLO-ALIAS` → `early`) | 0 |
| `bash scripts/preci.sh` | **PRE-CI PASS** — 10/10 gates obrigatórios | 0 |

`preci` (re-medido no mesmo archive) consolidou pytest hooks+scripts em **161 passed in 2.85s** e passou:

1. ZERO-ORFAOS (`validate_plugin.py`) — 5 dimensões, 74 `.md`
2. pytest hooks + scripts — 161
3. JSON dos 3 manifestos
4. paridade de versão `0.2.0`
5. `ruff check hooks/ scripts/`
6. gitleaks (archive sem `.git`: 0 commits, 0 leaks)
7. smoke offline (51 agents, 4 skills, 6 hooks executam)
8. drift semântico (51/51 agents, 4 skills, `porte-solo` limpo)
9. evals de roteamento (política pós-BT-5: `profiles=['early','scale','bigtech'] floor=early solo->early headcount_weight=0`)
10. `claude plugin validate` (marketplace `--strict` + plugin.json; warning conhecido de `CLAUDE.md` de processo, não FAIL)

HEAD citado: `4e8843f5b32ac2871bafe04cd1c8056487ed04c5`.  
Implementação: `7455adf` (16/08/26 23:57:59 −03). Tag de implementação (já existia, **não** recriada): `campanha/w3-bt5-porte-early` → `7455adf`.

**Item 4 = PASS.**

---

## Residuais (não-FAIL)

- **README** (`Structure … from solo founder to bigtech` / «do solo founder à bigtech») usa «solo» como *founder*, não como valor de `--porte`. Fora do âmbito `skills/hooks/agents`; o gate de produto não trata isso como peer de porte.
- **`docs/house/ORG.md`** ainda fala «NUNCA classificar como solo» (espelho vault, STALE face a BT-5). Excluído de `PRODUCT_FILE_GLOBS` / `docs/house/`. Cutover é BT-6, não esta fatia.
- **`docs/principles/agile-methodology.md`** tem «Time solo» como adaptação de ritos Scrum — capacity humana, não enum de porte.
- **`PLANO-MELHORIA-…`** e `phase0-metrics-before.json` descrevem o estado **antes** (prova histórica). Não são instrução operacional.
- Tag `campanha/w3-bt5-porte-early` marca a **implementação**. A tag desta verificação é `campanha/verify-bt5` no commit do relatório.

---

## Decisão

| Campo | Valor |
|---|---|
| Veredito | **PASS** (4/4) |
| TODO `BT-5` Status | **✅ Concluído** |
| TODO `BT-5` Estado Auditado | **✓** |
| Outros IDs | **intocados** (`BT-6`/`BT-7`/`BT-8` continuam 🔍; `TST-BT-1`/`AUD-BT-1` ⏳) |
| Push | **não** |
| Tag local | `campanha/verify-bt5` neste commit de verificação |
