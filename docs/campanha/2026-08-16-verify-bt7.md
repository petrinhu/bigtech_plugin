# BT-7 — verificação adversarial (gate de drift semântico)

**Data da verificação:** 2026-08-17  
**Hora local (medida):** `17/08/26 - 00:30:07` (`date '+%d/%m/%y - %H:%M:%S'`)  
**Papel:** verificador de campanha — host Grok, papel [grok][mais recente]  
**Item:** `BT-7` (drift gate semântico registry/agents/skills — CI/hook)  
**DoD desta ordem de serviço:**

1. `scripts/check_semantic_drift.py` existe e sai **exit 0** no repo
2. Integrado em `scripts/preci.sh` e `.github/workflows/ci.yml`
3. Testes em `scripts/tests` passam
4. **0** findings de drift no HEAD

**HEAD do plugin no início desta auditoria:** `9aadad19f8302219d7c3f3815c3e87760f2f4aa3` (`main` ahead 2 de `origin/main`; working tree limpa)  
**HEAD no fecho (pai deste commit):** `e1ff2a2369143f45cec1e20c2549e9ab951764fd` (`docs(bt-8): verificação evals de roteamento` — concurrente; **não** toca blobs do gate BT-7)  
**Implementação auditada:** `4ebacb0fc1e5c5bec5456126469411035372711f` (`feat(bt-7): gate de drift semântico registry/agents/skills`; tag anotada já existente `campanha/w4-bt7-drift-gate` → esse commit)  
**Prova de produto:** `git archive 9aadad1` → `/var/tmp/bt7-verify-HEAD-9aadad19f830` (não a working tree). Blobs do checker e da suíte **idênticos** a `4ebacb0` **e** a `e1ff2a2`. `preci.sh` e `ci.yml` mudaram depois de `4ebacb0` **só** para inserir o gate BT-8 (evals); o step/gate BT-7 ficou no sítio, obrigatório, sem `|| true`.  
**Método:** re-medição no archive + objeto git + enumeração independente (não só o exit code do script). Prosa do commit de implementação **não** foi aceite sem prova.

**Veredito global:** **PASS nos 4 itens.**  
**Decisão autônoma (confirmar retroativamente):** promover `BT-7` para **✅ Concluído** e Estado Auditado **✓**. Esta fatia **não** promove `BT-8` (já ✅ no `e1ff2a2` concurrente). Sem push. Sem patch de produto (verificador ≠ implementer).

---

## Escopo e o que isto não é

- Fecha a **prova** de que o gate offline existe, está no pre-CI e no Actions, a suíte `scripts/tests` cobre falha+integração, e o HEAD está limpo (0 findings).
- Fecha **preci local 10/10** neste SHA (gate 8 = BT-7).
- **Não** fecha TST-BT-1 (revalidação da campanha inteira no SHA final, pós BT-8) nem AUD-BT-1.
- **Não** é hook Claude Code (`SessionStart`/`PreToolUse`). O “hook” desta fatia é o gate 8 de `preci.sh` + o step hard do CI. Residual abaixo.
- **Não** houve push, merge, tag remota, release nem alteração de outros IDs **nesta** fatia. `BT-8` foi promovido noutro commit (`e1ff2a2`) enquanto esta auditoria corria.

---

## Checklist DoD (item a item)

| # | Item | Veredito | Evidência âncora |
|---|---|:---:|---|
| 1 | `scripts/check_semantic_drift.py` existe e exit 0 | **PASS** | tracked (`git ls-files`); blob `2bdccb83`; archive+working: exit **0** (verbose e `--quiet`); 51 agents / 4 skills / 6 scripts de hook |
| 2 | Integrado em `preci.sh` e `ci.yml` | **PASS** | `preci.sh` gate **8/10** obrigatório (`if ${PY} scripts/check_semantic_drift.py`); CI step hard no job nativo **e** no `test-distros` (debian/fedora/arch); sem `continue-on-error` no step; sem `\|\| true` |
| 3 | Testes em `scripts/tests` passam | **PASS** | archive e working tree: **11 passed** em `test_check_semantic_drift.py` (falha + integração no repo) |
| 4 | 0 findings de drift no HEAD | **PASS** | checker: `ok=True`, `len(findings)=0`; enumeração independente: **0** (name≠stem, dups, count, skills, hooks, codeberg, porte-solo) |

Nenhum FAIL material. Residuais abaixo **não** impedem ✅.

---

## 1. Script existe e exit 0 (item 1)

| Path | Linhas | SHA-256 (archive == working) | blob git HEAD |
|---|---:|---|---|
| [`scripts/check_semantic_drift.py`](../../scripts/check_semantic_drift.py) | 432 | `a670c42b4520f1eb2332e1528b0dd7859032137348b2fbf41937e694efc2f2ce` | `2bdccb8318e2e3e93d84e9acc51995b0b024c053` |
| [`scripts/tests/test_check_semantic_drift.py`](../../scripts/tests/test_check_semantic_drift.py) | 195 | `7e1d230eb1d8bb21cc8e72d383b260efb0f7829615de51963ad14d35c1faa63c` | `44422ae651b31ab51664457b8047fc80a055330b` |

`git ls-files` devolve os dois. `git hash-object` do archive == blob de `HEAD` == blob de `4ebacb0` (o checker **não** mudou depois da implementação).

Corrida no archive (`--root /var/tmp/bt7-verify-HEAD-9aadad19f830`):

```
== Drift semântico (BT-7) ==
root: /var/tmp/bt7-verify-HEAD-9aadad19f830
  · agents encontrados: 51 (esperado 51)
  · skills encontradas: 4
  · hooks type=command: 7; scripts resolvidos: 6
[PASS] drift semântico limpo (agents/skills/hooks/porte/host).
EXIT_CODE=0
EXIT_CODE_QUIET=0
```

Mesmo resultado no working tree (blobs idênticos). `EXPECTED_AGENT_COUNT = 51`. `NAME_STEM_EXCEPTIONS` vazio.

Checks hard do script (lidos no blob, não só no docstring):

1. `agents/*.md`: `name:` presente, único e == stem
2. `skills/*/SKILL.md`: `name:` presente
3. `hooks/hooks.json`: scripts em `args` existem
4. contagem == 51
5. `solo` não é porte primário (peer early/scale) sem alias/deprec
6. zero `codeberg.org` em paths de produto

---

## 2. Integração pre-CI + Actions (item 2)

### `scripts/preci.sh` (blob HEAD `f1ab1168`)

Gate 8 obrigatório, fail-fast (`set -euo pipefail`; `fail` aborta):

```
#   8. Drift semântico (BT-7)        -> python3 scripts/check_semantic_drift.py [obrigatório]
…
gate "Drift semântico (BT-7): check_semantic_drift.py"
if ${PY} scripts/check_semantic_drift.py; then
  pass "drift semântico limpo."
else
  fail "drift semântico reprovou — veja acima."
fi
```

`TOTAL_GATES=10` (era 9 no commit `4ebacb0`; o 10º é o gate BT-8 de evals, inserido em `b9bd2ee`). Diff `4ebacb0..HEAD` **não** remove nem suaviza o gate 8: só empurra `claude plugin validate` de 9 → 10. Sem `|| true` na linha do drift.

Corrida local neste SHA (`NO_COLOR=1 bash scripts/preci.sh`): **PRE-CI PASS — 10/10**. Gate 8 imprimiu o mesmo PASS do item 1.

### `.github/workflows/ci.yml` (blob HEAD `d937d74c`)

Dois steps hard, um por família de job:

| Job | Linha | Step |
|---|---:|---|
| `test` (ubuntu/windows × py 3.11/3.12) | 63–64 | `check_semantic_drift (BT-7)` → `python scripts/check_semantic_drift.py` |
| `test-distros` (debian 12 / fedora 41 / arch) | 158–159 | `check_semantic_drift (BT-7)` → `/tmp/venv/bin/python scripts/check_semantic_drift.py` |

O job `test` também chama `bash scripts/preci.sh` no Ubuntu (linha 98–100), então o gate 8 corre **duas vezes** no Linux nativo (step dedicado + preci). Não há `continue-on-error` em nenhum step; a única ocorrência da string no ficheiro é o comentário da linha 4: *“Sem continue-on-error nos gates obrigatorios.”*

Diff `4ebacb0..HEAD` no workflow: só adiciona o step BT-8 **depois** do BT-7, nos dois jobs. Drift permanece hard.

`hooks/hooks.json` **não** invoca o checker (7 blocos `type=command`, 6 scripts: `bigtech_session_init`, `bigtech_porte_reminder`, `tab_pendencias_reminder` ×2 eventos, `bigtech_reinforce`, `tdd_guard`, `tdd_runner`). Esperado: gate de repo, não hook de sessão.

---

## 3. Testes `scripts/tests` (item 3)

`python3 -m pytest scripts/tests -q` no archive e no working tree:

```
...........                                                              [100%]
11 passed in 0.29s
```

Casos (collect-only no archive):

| Teste | Papel |
|---|---|
| `test_clean_minimal_tree_passes` | árvore mínima 51 agents → ok |
| `test_agent_name_mismatch_fails` | `name:` ≠ stem → `agent-name` |
| `test_agent_name_duplicate_fails` | name duplicado |
| `test_agent_count_mismatch_fails` | 3 ≠ 51 → `agent-count` |
| `test_skill_missing_name_fails` | SKILL sem `name:` → `skill-name` |
| `test_missing_hook_script_fails` | args aponta para `.py` ausente → `hooks-scripts` |
| `test_codeberg_in_readme_fails` | host legado em README → `codeberg` |
| `test_solo_peer_porte_fails` | `early \| solo \| scale` sem deprec → `porte-solo` |
| `test_solo_deprecated_alias_allowed` | alias deprecado autorizado |
| `test_main_exit_codes` | `main()` 0 / 1 |
| `test_repo_root_integration` | repo vivo (no archive: raiz extraída) deve passar |

A suíte prova que o gate **falha** nos mutantes e **passa** no HEAD. Pytest completo do preci (hooks + scripts): **161 passed** (150 + 11).

---

## 4. Zero findings no HEAD (item 4)

Duas medições independentes no mesmo archive (`9aadad1`):

### 4.1 O próprio gate

`Report.ok is True`; `len(findings) == 0`. Notas: 51/51 agents, 4 skills, 7 command / 6 scripts.

### 4.2 Enumeração à parte (mesmo critério, parser próprio)

Hora da medição: `17/08/26 - 00:29:30`.

| Check | Resultado |
|---|---|
| `agents/*.md` | **51** |
| `name:` ≠ stem | **0** |
| `name:` duplicado | **0** |
| `name:` ausente | **0** |
| `skills/*/SKILL.md` | **4** (`bigtech`, `proj_software`, `tab_pendencias`, `visual-design-director`) |
| skill sem `name:` | **0** |
| scripts de hook em falta | **0** (6 ficheiros resolvidos) |
| ficheiros de produto varridos | **97** (mesmos globs + excludes do gate) |
| `codeberg.org` em produto | **0** |
| `solo` como porte primário sem deprec | **0** |
| **TOTAL independente** | **0** |

7 blocos `type=command` vs 6 scripts: `tab_pendencias_reminder.py` está em `SessionStart` **e** `UserPromptSubmit`. Não é finding.

**Item 4 = PASS.**

---

## Residuais (não-FAIL)

- **Não é hook Claude Code.** A descrição do item diz “CI/hook”. A entrega é gate de pre-CI (`preci.sh`) + step de Actions. `hooks/hooks.json` não chama `check_semantic_drift.py`. Isto é **desenho**, não buraco: o gate é de inventário do *repo*, não de turno de sessão.
- **CHANGELOG Unreleased** lista BT-8 e BT-5 e **omite** BT-7. Verificador não patcha changelog.
- **`docs/campanha/` e `docs/house/`** estão em `PRODUCT_EXCLUDE_PREFIXES` (prova histórica / espelho vault). O próprio script e `scripts/tests/` também, para não auto-alarmar. A enumeração independente usou a mesma exclusão.
- Tag de implementação já existia: `campanha/w4-bt7-drift-gate` (tag object `92ead802…` → commit `4ebacb0`). Esta verificação cria `campanha/verify-bt7` no commit do relatório.

---

## O que esta verificação não promove

- `BT-8` **não** é promovido aqui. No fecho, já está **✅** por `e1ff2a2` (outro verificador). Blobs de evals não foram re-auditados nesta fatia.
- `TST-BT-1` / `AUD-BT-1` ficam **⏳**.
- CI remoto (Actions verde no `origin`) **não** é DoD desta fatia — o workflow está no blob; a corrida remota é TST-BT-1 / BT-4.
- Tag remota / push / release: **não**.

---

## Decisão

| Campo | Valor |
|---|---|
| Veredito | **PASS** (4/4) |
| TODO `BT-7` Status | **✅ Concluído** |
| TODO `BT-7` Estado Auditado | **✓** |
| Outros IDs | **intocados neste commit** (`BT-8` já ✅ em `e1ff2a2`; `TST-BT-1`/`AUD-BT-1` ⏳) |
| Push | **não** |
| Tag local | `campanha/verify-bt7` neste commit de verificação |

**Decisão autônoma (confirmar retroativamente):** PASS material. `BT-7` → **✅ Concluído** + Estado Auditado **✓** no mesmo commit deste relatório. Tag local `campanha/verify-bt7`. Sem push.
