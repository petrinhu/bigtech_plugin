# BT-6 — verificação adversarial (inventário dual-authority + plano)

**Data da verificação:** 2026-08-17  
**Hora local (medida):** `17/08/26 - 00:25:04` (`date '+%d/%m/%y - %H:%M:%S'`)  
**Papel:** verificador de campanha — host Grok, papel [grok][mais recente]  
**Item:** `BT-6` (inventário dual-authority agents: classificar CORE/OVERLAY/STALE/EXCLUSION + plano cutover)  
**DoD desta ordem de serviço:**

1. Existem e são coerentes: `docs/campanha/2026-08-16-dual-authority-inventory.md` + CSV
2. Existe e é só plano: `docs/campanha/2026-08-16-dual-authority-cutover-plan.md`
3. Taxonomia do ADR-source-of-truth (D8) está referenciada e aplicada
4. Decisão: **sem cutover real** de `~/.claude` (só plano)
5. Contagens INTERSECTION / VAULT_ONLY razoáveis contra re-medição rápida

**HEAD do plugin no início desta auditoria:** `4e8843f5b32ac2871bafe04cd1c8056487ed04c5` (`main` == `origin/main`, working tree limpa)  
**Commit da entrega BT-6:** `9d814a8891b4a886788bc0f866bbffea4a5f6ee2` (`docs(bt-6): inventário dual-authority + plano de cutover`)  
**SHA citado na medição original:** plugin `77c1f9e426ead89384f5997cd7c8bccc3e16f8a0`; vault `claude-memory` `5b19b524a7409d5b60ea3a3912b2761877c6c7b8`  
**Método:** re-medição em disco (`name:` no frontmatter YAML de `*.md`, sha256, conjuntos) + objeto git + `~/.claude` / `~/.grok` ao vivo. Prosa do inventário **não** foi aceite sem prova.

**Veredito:** **PASS em todos os itens materiais do DoD desta ordem.**  
**Decisão autônoma (confirmar retroativamente):** promover `BT-6` para **✅ Concluído** e Estado Auditado **✓**. Não é promoção de BT-5 / BT-7 / BT-8 (permanecem 🔍). Não executa PHASE 11. Sem push.

---

## Escopo e o que isto não é

- Fecha a **prova** de que o inventário classificado (51+20) e o plano de cutover existem, batem com a taxonomia D8 e com o disco vivo, e que **ninguém removeu** globais em `~/.claude/agents/`.
- **Não** é o cutover (PHASE 11). Plano ≠ ordem de apagar.
- **Não** fecha TST-BT-1 / AUD-BT-1 (W6/W7).
- **Não** reclassifica ficheiro a ficheiro (verificador ≠ implementer). Residual de prosa no §3 do inventário fica documentado, não corrigido aqui.
- **Não** houve push, merge, tag remota, release nem edição de `~/.claude/agents`.

---

## Checklist DoD (item a item)

| # | Item | Veredito | Evidência âncora |
|---|---|:---:|---|
| 1 | Inventário `.md` + CSV existem e são coerentes | **PASS** | tracked no `9d814a8`; 195 + 72 linhas; 71 linhas de dados; conjuntos md×csv×disco = 0 delta de nomes |
| 2 | Plano de cutover existe e é só plano | **PASS** | `2026-08-16-dual-authority-cutover-plan.md` (223 linhas); título “só plano”; §0/§6/§8 proíbem execução |
| 3 | ADR-source-of-truth D8 referenciado e aplicado | **PASS** | links `docs/adr/ADR-source-of-truth.md`; classes D8 só; 51 CORE-GENERIC + 12 EXCLUSION + 8 OVERLAY; nenhum STALE de ficheiro inteiro |
| 4 | Sem cutover real de `~/.claude` | **PASS** | vault ainda 71 `*.md`; 0 `git log --diff-filter=D` em `agents/` desde 2026-08-01; working tree `agents/` limpa; mtimes Jul/25 |
| 5 | INTERSECTION / VAULT_ONLY vs re-medição | **PASS** | vivo: plugin 51, vault 71, INTERSECTION **51**, VAULT_ONLY **20**, PLUGIN_ONLY **0**; sha256 plugin/vault vs CSV: **0** mismatch |

Nenhum FAIL material. Residual abaixo **não** impede ✅.

---

## 1. Artefactos (existência + blobs)

| Path | Linhas | SHA-256 (disco) | blob git HEAD |
|---|---:|---|---|
| [`docs/campanha/2026-08-16-dual-authority-inventory.md`](2026-08-16-dual-authority-inventory.md) | 195 | `5ffd629e44faa72d085fc410f719730be8f462add297d62cd8a985f4440a67c6` | `5ca41da4130b6beecd08bd1362872ccd98747041` |
| [`docs/campanha/2026-08-16-dual-authority-inventory.csv`](2026-08-16-dual-authority-inventory.csv) | 72 | `9b78d3a81500cb6b7ba00929adda9b3769438905fc968f96ccf3676ecf040dad` | `343e2239607153fdeaca162ab1eb2df1365375e3` |
| [`docs/campanha/2026-08-16-dual-authority-cutover-plan.md`](2026-08-16-dual-authority-cutover-plan.md) | 223 | `61b49322cb6e955eda8562271ecf8c3674d5740df64bcf5c10ca0aae0ab4c535` | `3f6be82c448e4a0dd30204c067d922d55f3e670e` |
| [`docs/adr/ADR-source-of-truth.md`](../adr/ADR-source-of-truth.md) | 366 | `801d89419ead97198f0f02209ef505174ed2003ee1b4bfa62a58a9ae6ce8a70c` | `515ce2960b4aee9f5c24a35d38316506cbd76ab7` |

`git ls-files` devolve os quatro. Nenhum untracked nesta fatia de verificação (antes deste relatório). Tag de entrega já existia: `campanha/w3-bt6-dual-authority` → `9d814a8`.

Commit `9d814a8` (4 files, +491/−1): os 3 artefactos BT-6 + `TODO.md` → 🔍. `agents/` do plugin **não** mudou nesse commit nem desde ele (`git log 9d814a8..HEAD -- agents/` vazio). Blob `agents/celso-ceo.md` em `77c1f9e` == HEAD (`ea6df418…`).

---

## 2. Taxonomia ADR (item 3)

ADR D8 (quatro classes, uma por diferença, antes de qualquer port):

| Classe D8 | Destino | Aplicação BT-6 medida |
|---|---|---|
| `CORE-GENERIC` | plugin | 51 / 51 da INTERSECTION |
| `PERSONAL-OVERLAY` | vault | 8 / 20 VAULT_ONLY |
| `INTENTIONAL-EXCLUSION` | vault | 12 / 20 VAULT_ONLY |
| `STALE` | remover lado defasado | **0** ficheiros inteiros; só resíduo *dentro* da cópia vault |

Inventário §1 e plano §0 citam o ADR por path relativo. Plano consome D1–D8 + D6 rollback. Ações por linha do CSV:

| `cutover_action` | N |
|---|---:|
| `plugin wins; depois delete vault copy` | 51 |
| `keep intentional` | 12 |
| `vault overlay` | 8 |

Heurística PHASE 0 do ADR (interseção sugerida CORE-GENERIC; jogo → EXCLUSION; perícia/host/coach/PMM → OVERLAY) **revalidada**: 12 exclusões = 10 stack jogo + `narrative-writer` + `learning-designer`; 8 overlay = 4 `dr-*` + `linux-diag` + `engineering-coach` + `product-marketing-manager` + `revisor-textual`. Nenhum `VAULT_ONLY` classificado `CORE-GENERIC`. Nenhum agent do pacote 0.2.0 ficou `PLUGIN_ONLY`.

---

## 3. Re-medição rápida (item 5)

Parser: `name:` no frontmatter YAML de `*.md` (não o filename). Hora da medição: `17/08/26 - 00:23:50`.

| Superfície | Path | Ficheiros | `name:` distintos |
|---|---|---:|---:|
| Plugin | `agents/` deste repo | 51 | 51 |
| Vault Claude | `~/.claude/agents/` | 71 | 71 |
| Grok (não 3ª SoT) | `~/.grok/agents/` | 71 | 71 |

| Conjunto | Inventário / CSV | Disco agora | Delta |
|---|---:|---:|---:|
| INTERSECTION | 51 | 51 | 0 (nomes idênticos) |
| VAULT_ONLY | 20 | 20 | 0 (nomes idênticos) |
| PLUGIN_ONLY | 0 | 0 | 0 |
| sha256 plugin == vault (INTERSECTION) | 0 / 51 | 0 / 51 | 0 |
| sha256 plugin == grok | 0 | 0 / 51 | 0 |
| sha256 vault == grok | 0 | 0 / 71 | 0 |
| CSV vs disco (sha256 plugin) | — | 0 mismatch | — |
| CSV vs disco (sha256 vault) | — | 0 mismatch | — |
| C-level na interseção | 12 | 12 | 0 |
| Operacionais na interseção | 39 | 39 | 0 |
| Plugin mudou desde PHASE 0 | 4 | 4 (mesmos nomes) | 0 |
| Vault mudou desde PHASE 0 (coluna CSV) | 0 | 0 (hashes vault == CSV) | 0 |

Os 4 hashes de plugin distintos da PHASE 0 (ainda `CORE-GENERIC`): `cosimo-chief-of-staff`, `devops-sre`, `qa-engineer`, `security-engineer`. Vault **não** absorveu BT-5: prova viva de dual authority, ainda verdadeira hoje.

VAULT_ONLY vivo (20, bytes batem com o inventário):

`3d-artist-rigger` 11571, `audio-designer-composer` 13268, `economy-designer` 12776, `engine-graphics-programmer` 13195, `game-animator` 12969, `game-producer` 13673, `gameplay_engineer` 9162, `lead-game-designer` 12742, `level-designer` 12135, `narrative-designer` 19393, `narrative-writer` 23205, `learning-designer` 15125, `dr-advogado` 15995, `dr-medico-perito` 16485, `dr-medico-psiquiatra` 16751, `dr-medico-trabalho` 18170, `linux-diag` 4722, `engineering-coach` 12672, `product-marketing-manager` 3127, `revisor-textual` 12813.

---

## 4. Sem cutover real (item 4)

Provas de que `~/.claude/agents/` **não** foi cortado:

| Checagem | Resultado |
|---|---|
| Contagem viva | **71** `*.md` (51 core + 20 vault-only) |
| `git ls-files agents/*.md` em `~/.claude` | **71** |
| HEAD `claude-memory` | `5b19b524a7409d5b60ea3a3912b2761877c6c7b8` — **igual** ao SHA da medição BT-6 |
| `git log --since=2026-08-16 -- agents` | **vazio** |
| `git log --diff-filter=D --since=2026-08-01 -- agents` | **vazio** |
| `git status -- agents` | limpo (working tree de agents sem mudança) |
| Dirty em `~/.claude` | só memória de *projeto* (`projects/…/memory/*`); **não** agents |
| mtime mais recente em `~/.claude/agents/*.md` | 25 Jul (ex.: `devops-sre.md`) — anterior à campanha 16/08 |
| Homônimos ativos | os 51 `name:` do plugin **ainda** existem no vault |

O plano §0 / §6.1 / §8 e o inventário §0 / §8 escrevem a decisão: **não** apagar, mover, renomear nem desativar `~/.claude/` (nem `~/.grok/`) nesta fatia. Canário = PHASE 11, depois de go do líder. Estado transitório (51 homônimos) aceite (ADR D2).

`~/.grok/agents/` também intacto como terceira cópia (71 ficheiros, 0 sha256 iguais ao plugin ou ao vault). Fora do DoD de cutover *desta* fatia; o plano Passo G deixa explícito “Não nesta fatia”.

---

## 5. Coerência do plano (item 2)

O plano **não** executa. Checklist lido:

- Pré-condições 1–8: canário, CI, FABLE-CUTOVER, autorização do líder **antes** de qualquer `git rm`.
- Passos A–H ordenados; `rm` só no Passo D, e só no repo versionado `claude-memory`, depois do canário isolado (`CLAUDE_CONFIG_DIR` ≠ `~/.claude`).
- Rollback (Passo R) obrigatório **antes** do Passo D no vivo; proíbe `git reset --hard` e `isolation: worktree` sob IDrive.
- §6 lista 12 proibições, incluindo “Não cutover de `~/.claude` nesta fatia”, “Não daemon/sync bidirecional (D7)”, “Não união vault ∪ plugin”.
- DoD da fatia BT-6 ≠ DoD da PHASE 11. Checkboxes da entrega (inventário + plano + decisão escrita) estão marcados; execução PHASE 11 não está.

---

## Residual (não bloqueia)

Inventário §3 (resumo em prosa) diz `wikilinks: 40 / 51` e `caveman: 12 / 51`.  
CSV + re-medição viva + tabelas por linha do próprio `.md`:

| Marcador | §3 prosa | CSV / disco / tabelas |
|---|---:|---:|
| `identity_lider` | 51 / 51 | 51 / 51 |
| `wikilinks` (`[[…]]`) | **40** / 51 | **42** / 51 |
| `caveman`/`cavecrew` | **12** / 51 | **11** / 51 |

A lista nominal de caveman no §3 tem **11** nomes + a frase “+ refs `cavecrew`” (contagem inflada). Os 11 nomes batem com o CSV. `wikilinks` no CSV = 42; `[[` literal no vault da interseção = 42; extra vs prosa = 2, sem nome em falta no CSV.

Isto é **erro de resumo**, não de classificação nem de conjunto. A prova operacional é o CSV (71 linhas, hashes vivos). Verificador **não** patcha o inventário.

---

## O que esta verificação não promove

- `BT-5`, `BT-7`, `BT-8` ficam **🔍** (outra ordem de serviço).
- `TST-BT-1` / `AUD-BT-1` ficam **⏳**.
- PHASE 11 / FABLE-CUTOVER **não** abre com este PASS.
- Tag remota / push / release: **não**.

---

## Decisão (confirmar retroativamente)

PASS material. `BT-6` → **✅ Concluído** + Estado Auditado **✓** no mesmo commit deste relatório. Tag local `campanha/verify-bt6`. Sem push.
